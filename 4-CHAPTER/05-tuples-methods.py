# Creating a tuple using ()
t = (1, 2, 3, 4, 5,2,2,2) # its a tuples but we cannot update tuples

### for getting no of occurances of a value then we can used tuple.count(value)
print(t.count(2))

### for getting value of specific index we used tuple.index(index)
print(t.index(2))